<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="premiereapplication.automation.test.notreapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Register"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Login1" />
        <activity
            android:name=".Categorie_activity"
            android:label="@string/title_activity_categorie_activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Main_Activity_product" />
        <activity
            android:name=".ProductList"
            android:label="Product List"
            android:parentActivityName=".Main_Activity_product" />
        <activity android:name=".RegistreVolley" />
        <activity android:name=".Acheter_produit" />
        <activity android:name=".A_propos" />
        <activity android:name=".Encherir"></activity>
    </application>

</manifest>